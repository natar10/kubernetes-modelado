# =============================================================================
# BASE DE DATOS - Keycloak
# =============================================================================
# PostgreSQL dedicado para Keycloak
# - Almacena configuración de realms
# - Usuarios y credenciales OAuth2/OIDC
# - Sesiones y tokens
# Tipo de servicio: ClusterIP (solo accesible internamente)
# =============================================================================

---
# PersistentVolumeClaim para persistencia de datos
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-keycloak-pvc
  namespace: eventos-system
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/name: db-keycloak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
# Deployment de PostgreSQL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-keycloak
  namespace: eventos-system
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/name: db-keycloak
    app.kubernetes.io/part-of: eventos-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-keycloak
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-keycloak
        app.kubernetes.io/component: database
        app.kubernetes.io/name: db-keycloak
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - secretRef:
                name: db-keycloak-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - keycloak_user
                - -d
                - keycloak_db
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - keycloak_user
                - -d
                - keycloak_db
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: db-keycloak-pvc

---
# Service ClusterIP para acceso interno
apiVersion: v1
kind: Service
metadata:
  name: db-keycloak
  namespace: eventos-system
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/name: db-keycloak
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: db-keycloak
